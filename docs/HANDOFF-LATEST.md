# 최신 인수인계

## V96 비동기 서보 피드백 — 코드·빌드 완료, 다른 컴퓨터에서 설치 예정 (2026-09-23)

[구현·검증·설치 인계 문서](ASYNC-FEEDBACK-V96-2026-09-23.md).
로봇은 꺼져 있으며 이번에는 연결/설치하지 않았다. 공유 보행의 동기 조회 대기를
USART1 IRQ 상태기로 분리하고 재시도는 다음 프레임 이후로 미뤘다. 50Hz는 유지한다.
RXNE와 함께 발생한 UART 오류를 보존하고 정지 후 `busdiag`로 시도별 기록을 읽는다.
응답 누락/지연을 주입한 호스트 모형에서 명령 간격20ms를 확인했다.
관련 검사141개 통과/13개 skip, 그13개와 프로필 검사1개는 Zig로 별도 실행해 통과했다.
이는 실기46ms 문제가 해결됐다는 측정 결과는 아니다.

설치 파일은 `artifacts/async-feedback-v96/firmware/attitudepd-v9-v96-async-feedback.bin`.
327328B, SHA256 `fd70b04878976a5a4e0e2e1aed88672fbb3d2df610f6e43a66c2ae746a155126`.
전 축ACC254와 기존V10 실험 소스를 포함한다. V10 보행 문제는 미해결이다.
마지막 설치 확인은V93(67)이다. 새 컴퓨터에서 Landing 실제 도착 → 토크OFF →
설치 → Landing 확인을 하고, 동일 보행 입력의 jointtrace/busdiag를 비교한다.
아래의 분석·V95 빌드 기록은 변경 전/이전 단계의 이력이다.

## 46ms 명령 간격 원인 분석 (2026-09-23)

[원시 사건·원인별 판단·검증 계획](CONTROL-GAP-46MS-2026-09-23.md).
V70의 두 46ms 구간은 각각 RR J2/RL J3 상태 조회가 31ms 걸린 구간과 일치했다.
송신 1ms + 조회 33ms + 미분리 처리 12ms다. 25ms 타임아웃/동기 재시도가
20ms 제어 주기를 막는 구조를 확인했다. RXNE를 오류보다 먼저 처리해 동시
ORE/FE/NE를 놓칠 수 있는 결함도 찾았지만 당시 오류 발생 기록은 없어
최초 실패 원인은 미확정이다. 5개 원시 캡처와 CSV 시간 대조 통과.
결과는 `artifacts/control-rate-review-2026-09-23/gap-analysis.json`.
펌웨어 동작 변경·설치·새 실기 시험은 하지 않았다.

## 제어 주기 조사 (2026-09-23)

[조사·실기 기록 재계산](CONTROL-RATE-REVIEW-2026-09-23.md). 현재50Hz 유지,
통신/연산 개선 뒤100Hz를 우선 비교 후보로 판단했다. 최적값 확정은 아니다.
5개 과거 실기 캡처에서 명령 간격 중앙값20ms, 일부 최대46ms/조회31ms를
확인했다. 프레임 기반 필터·미분·보호·자세 전환과 시뮬레이터를 함께 맞춰야
주파수만 다른 공정한 비교가 된다. 이번 조사는 실기 명령·주기 변경 없이 수행했다.

## ACC254 요청 통일 — 코드·빌드 완료, 실기 미설치 (2026-09-23)

[변경과 검증 기록](ACC254-2026-09-23.md). 사용자 요청으로 J1/J3의 ACC50
소프트웨어 상한을 제거했다. 기본 보행은 전 축254를 요청하며 낮은 명시값과
실제 레지스터 readback 검사를 유지한다. 시뮬레이터 기본값도254이다.
과거50 readback 재현은 명시적인 파라미터와 회귀 시험으로 보존한다.
펌웨어 `attitudepd-v9-v95-acc254`, 최종 산출물은
`artifacts/acc254-2026-09-23/firmware-ready/`이다. 327400B, 슬롯 잔여280B.
기존 V10 코드까지 포함한 빌드의 용량 초과를 해결하기 위해 actuator/heading
계산에 크기 최적화를 적용하고 관련 호스트 검사를 수행했다.
실제 로봇 연결·설치·readback·동작 검증은 하지 않았다.
전 축254 가정의 V10 한 걸음은3.86초에 tilt로 중단되어 보행 성공으로 취급하지 않는다.

그 전에 수행한 [SyncWritePosEx 비교](SYNCWRITEPOSEX-ASSESSMENT-2026-09-23.md)는
ACC50 기준과 J3만254인 가정을 비교한다. 이번 전 축254 시험과 구분한다.
현재 서보 송신은 `HAL_UART_Transmit()` 동기 방식이며 DMA 전환은 구현하지 않았다.

## V10 왼발 선행 한 걸음 — 오프라인 48조건, 채택값 없음

[요청한 동작 순서·탐색 결과·영상·한계](NAVIGATION-V10-ONE-STEP-SEARCH-2026-09-22.md).
오른쪽 준비 이동은 취소됐다. 왼발 착지 이후 오른발 밀기를 끝내고,
왼발 J1으로 몸체 이동을 받아주면서 오른발을 들어 Stand로 회수하는 한 걸음을 시험했다.
48조건 모두 정상 순서의 왼발 동시 이륙을 충족하지 못했다. 최적값으로 채택/실기 설치하지 않았다.
V10은 소스의 실험 프로필이며 반복 보행·실제 접지 기반 전환은 미완성이다.
실험 파라미터는 오프라인 실행에서만 명시적으로 설정한다. 판정은 validated-results.json을 참고한다.
커밋 전 관련 검사293개 확인: 처음292개 통과, V10에 전진을 넣던 공유 커널 비교 시험1개는
왼쪽 전용 입력과 CAD 서보 인코딩으로 수정 후 재실행 통과했다.
사용자의 마지막 미해결 질문은 “왜 뒤로 가는가?”이다. 대표 시험 X−71.8mm 후퇴가
기록됐으나 앞뒤 힘·미끄러짐·관절 추종 오차의 원인별 기여 분석은 아직 완료하지 않았다.

## V95 (69): V9 같은 쪽 페어 구현·비교 — V8보다 나쁨, 실기 미설치

[V8/V9 비교와 실패 기록](NAVIGATION-V9-V95-2026-09-22.md).
FL+RL / FR+RR 교대와 네 발 지지 중 Y 무게 이동60mm를 추가했다.
V8과 같은2.4초 주기에서는 실패,4.8초로 늦추면 종료하지만 뒷발 접지가 지속된다.
좌우 저장 보정 시험의 두 발 동시 들림 지표0%, yaw 범위약19°로 V8보다 나쁘다.
V9은 실험 목록/default에 추가했으나 실기 채택 권장 모델이 아니다.
코드·Windows·STM32 빌드 및 관련288개 시험 통과. 최종 비교 영상은
`artifacts/navigation-v9/comparison-right.mp4`, 상세 기록은 `final/`이다.
실기와 설치 Windows는 여전히 V93(67), 기존 열린 MuJoCo는 V8이다.
이번 작업에서 장치 설치·실기 명령·기존 앱/뷰어 재시작은 하지 않았다.

## V94 (68): V8 대각선 두 발 옆걸음 — 코드/빌드 완료, 접지 검증 미완료

[구현·영상·실패 기록·남은 문제](NAVIGATION-V8-V94-2026-09-22.md).
V7을 보존하고 V8을 추가했다. FL+RR / FR+RL 교대, 0.6초 스윙/0.6초 네 발 지지,
초기1.5초 진입과 공통 들림 파형을 구현했다. 저장 보정으로 좌우20초 보행/정지는
보호정지 없이 완료했지만 접지 지연·roll 흔들림이 남는다. 보정0에서는 발끌림이
남고 반입력에서는 앞뒤 이동이 커져 전체 검증 통과로 취급하지 않는다.
최종 영상/데이터는 `artifacts/navigation-v8/v94/`, 빌드는 `firmware-final/`,
`windows-dist/`다. **실기/설치 앱은 여전히 아래 V93(67)**이며 V94를 설치하지 않았다.
관련273개 테스트 통과, Apple 빌드 미수행. 자동 하드웨어 시험을 이어서 실행하지 말 것.

## V93 (67): V7 조종 방향 정책 · 실기/Windows 설치 완료

[구현·검증·남은 한계](NAVIGATION-V7-V93-2026-09-22.md).
IMU 직진 보정, 방향 유지 옆걸음, 앞 대각선 전진 회전,
뒤 대각선 제자리 회전, 정후진 보행 주파수50%를 별도 V7에 구현했다.
STM32 V93 및 Windows V93(67) 빌드 완료. Apple 소스 갱신, 빌드 미수행.
사용자의 설치 요청에 따라 `artifacts/navigation-v7/install-01/`에서 Landing 도착
(24틱) → 12축 토크 OFF → OTA 이미지 검증·재부팅 → V93 Landing 도착(23틱)을
확인했다. V7 기본 모델, 서보 영구 레지스터, 들림20/20/20/20mm 및
간격-30/-30/+30/+30mm(FL/FR/RL/RR)을 보존했다. 실제 하중 보행은 미수행이다.
최초 설치 후 검증은 ID12 온도150°C 단발값으로 실패했다. 이 실패 기록을
보존하고, 동작 없이 3회 재조회해45/46/45°C, 전체12축 정지·hw오류 없음,
Landing/22틱/토크ON/safetyOK를 확인했다. 단발 이상값 원인은 미확정이다.
종합 증거는 `install-01/completion.json`; 원래 `post-verify.json`은 실패 그대로다.
Windows는 `%LOCALAPPDATA%/Programs/SpotOMG/V93-67/`에 설치·실행했다.
229개 파일 해시 대조 완료, 바탕화면 `Spot OMG.lnk`도 V93으로 교체했다.
새 Windows 앱의 BLE 연결, 전용 제어 채널, V93/V7/Landing/안전OK 표시도 확인했다.

## V92-R2 (66): 앞다리 간격 부호 수정 · 실기/Windows 설치 완료

[원인과 검증](FOOT-WIDTH-FRONT-SIGN-V92-R2.md). 공통 간격 보정의 앞 J1 모터/CAD
변환 누락을 수정했고 관련31개 호스트·기구학·UI 시험을 통과했다.
Windows R2 (66) 설치·실행 및 바탕화면 바로가기 교체 완료.
초기 Landing은 IMU 오류로 실패해 설치를 중단했고, 사용자 전원 재시작 후
install-r2-05에서 Landing 도착(27틱) → 12축 토크 OFF → OTA 검증·재부팅 →
R2 Landing 도착(22틱)을 확인했다. 서보 영구 설정과 높이·간격 저장값 보존 확인.
확인 종료 시 Landing/토크 ON/safety OK, 들림20/20/20/20mm,
간격-30/-30/+30/+30mm(FL/FR/RL/RR). 실제 보정 보행·위치 유지 시험은 미수행.
Apple 소스92.2.0/build66은 준비했으나 Apple 앱 빌드·설치는 하지 않았다.

## V92-R1 (65): 발 위치 보정 · 실기/Windows 설치 (2026-09-22)

[발 위치 보정 기록](FOOT-POSITION-CORRECTION-2026-09-22.md)을 먼저 참고한다.
네 발의 추가 들림과 좌우 간격(0 기본/음수 안쪽/양수 바깥쪽)을 함께 저장한다.
로봇 `attitudepd-v6-v92-r1` OTA 및 Windows `V92-R1 (65) - Release` 설치 완료.
Landing 도착 → 12축 토크 OFF → 설치 → Landing 재확인 절차를 지켰다.
확인 종료 시 Landing/오차 24틱/토크 ON/safety OK. 들림은 모두10mm, 간격은 모두0mm.
기존 서보 영구 설정 보존을 확인했다. 간격 보정의 실기 보행 효과는 아직 미검증이다.
Apple 소스는 같은 버전으로 변경했으나 이번 iPhone/Mac 빌드·설치는 하지 않았다.
Windows 바탕화면 바로가기는 `%LOCALAPPDATA%/Programs/SpotOMG/V92-R1-65/SpotOMGController.exe`를 가리킨다.

## V92: IMU 자세 안정화 V6 / 공용 앱 업데이트 (2026-09-22)

아래 V90-R1 및 Windows 0.1.x 항목은 이전 기록이다. 현재 소스 버전은
펌웨어 `attitudepd-v6-v92`, Apple 앱 92.0.0(64), Windows 앱 92.0.0이다.
STM32 발높이 영구 저장, IMU 읽기 실패 3초 후 자동 복구 및 수동 복구,
전용 제어/진단 분리, ±20도 조이스틱 보정과 앱 표시·아이콘을 반영했다.

- [V6 복귀 궤적·검증 한계](ATTITUDE-V6-RECOVERY.md): J2/J3 회수 추종을 개선한다.
  중간 밀기 속도는 V5와 같지만 주기가 25% 길고 지지 평균 속도는 20% 낮다.
  앞발만 +30mm인 중간 입력의 기울기 중단이 남아 있으며 실기 보행은 미검증이다.
- **로봇 V92와 Mac 앱 설치 완료.** 정상 Landing 도착 → 12축 토크 OFF → OTA 검증·재부팅 →
  정상 Landing 도착을 확인했다. 설치 확인 종료 시 Landing/error 7틱/토크 ON/safety OK,
  발높이 0/0/0/0 및 서보 영구 설정 보존. 이후 실제 상태는 새로 조회해야 한다.
- [Windows V92](WINDOWS-V92-PARITY.md): 다크 UI, 모드 탭·모델명, 로봇 윗모습,
  별도 진단 기록과 수동 IMU 복구 흐름 반영. 최종 앱 시험 242개 통과.
  **Windows 실행 파일 빌드·PC 설치 및 iPhone V92 설치는 아직 하지 않았다.**
- [발높이 저장](FOOT-LIFT-PERSISTENCE-V90-R2.md),
  [IMU 자동 복구](IMU-AUTO-RECOVERY-V90-R3.md),
  [Apple 제어 채널·전압·진단 기록](APPLE-CONTROL-CHANNEL-2026-09-21.md).

## Windows 0.1.12: 정지 기준 배터리 안내

사용자가 순간 전압강하로 인한 잦은 배터리 경고를 지적했다. 보행 중 `$BATTERY`
및 종료 진단의 `min_voltage`는 충전 경고/표시에서 제외하고 로그에만 남긴다.
정지 완료 후 3초 대기, 직접 요청한 `read 1`의 3개 샘플(기본 5초 간격,
범위200mV 이하)의 중앙값으로 표시와 경고를 갱신한다. 새 동작은 샘플을 초기화하며
보행 중 마지막 정지 전압을 유지한다. 기존 펌웨어 저전압 보호는 변경하지 않았다.
최종 관련 82개 검사 통과. 전체 Windows 116개 검사도 표시 갱신 보완 직전 통과했다.
새 실행 파일: `apps/windows/dist/controls-0112/SpotOMGController/SpotOMGController.exe`.
0.1.12 교체·실행 및 실제 BLE 연결 완료. 18:09 정지 read1 3회 11200mV,
앱의 정지 기준 전압 약11.2V 표시 확인. 최신 실기 상태는 torque ON/custom,
safety=tilt/fault_code14(사용자 보행 후)이며, 아래 설치 직후 torque OFF와 구분한다.
iPhone 배터리 정책은 이번 Windows 변경에 포함되지 않았다.

## 최신 추가: 보호정지 일시정지 / Windows 0.1.11

[보호정지 재시도 및 경고 UI](PROTECTIVE-PAUSE-V90-R1.md)를 먼저 확인한다.
코드·테스트·빌드 및 **새 펌웨어 실기 설치 완료**. Windows 앱 0.1.11 교체·실행.
사용자 Landing 근처 재시작 후 install-02에서 Landing 완료(error=21),
12축 토크 OFF, OTA 이미지 검증·재부팅, 설치 후 Landing 및 서보 저장 설정
유지 검증에 성공했다. 종료 시 torque OFF·safety OK(custom error=86).
실제 보호정지 후 재시도 보행 시험은 아직 하지 않았다.
아래 torque OFF는 이전 설치 직후 기록이다. 이후 사용자 조작 중 torque ON,
custom에서 기울기 보호정지가 관측되었으므로 현재 상태는 다시 조회해야 한다.

**현재 작업 명칭: V90-R1 (사용자 지정).**
IMU 진단/수동 I2C 복구 펌웨어 **실기 설치 완료**. Landing/서보 설정 유지/12축
토크OFF 확인. 복구 명령은 I2C 재초기화·ID·모드 확인 성공, body 검증 실패로
최종 FAIL이다. 고장 상태 복구 성공으로 해석하지 않는다. 현재 토크OFF.
[IMU 복구 검증·설치 상태](IMU-BUS-RECOVERY-V90-R1.md)를 참고한다.
Windows 최신 UI는 0.1.10이다. 발 들림 창을 밝은 배경으로 수정하고 연결 후
저장된 보정값을 정지 상태에서 한 번 자동 적용한다. 새 readback 일치 후 완료 처리.
창 재열기 시 미반영 편집은 버리고 로봇 적용값을 표시한다. 연결 전에는 저장값임을
표시한다. 관련 5개 검사 통과. 실행 경로는 `apps/windows/dist/controls-0110/SpotOMGController/SpotOMGController.exe`.
Windows 최신 앱은0.1.9: 정지 응답/상태 조회 대기 중 새로 잡은 스틱 입력이
누락되지 않도록 수정했다. 완료 후에도 현재 잡고 있는 최신 입력만 사용하며,
놓은 입력은 재생하지 않는다. 보호 정지 때 계속 잡은 입력은 자동 재시작하지 않는다.
106개 앱 검사 통과, 제어 경로 10개 검사(앱 8 + STM32/ESP32 호스트 2) 통과.
실제 무선 지연/보행 재검증은 아직 하지 않았다. 아래 인수인계의 0.1.9 절 참고.
후속 요청으로10mm 입력 상한을 제거한 Windows0.1.7/펌웨어를 준비했고,
IMU 연결 복구 후 로봇 설치까지 완료했다. 실제20/30/256/300mm 설정·조회 및
잘못된 값의 원자적 거절을 확인한 뒤 모두0mm로 복구했다. 보행 효과는 미검증.
현재 토크 OFF. 같은 버전명이므로 최신 이미지 해시085fa9d1…로 구분한다.
[V90-R1 병합/설치 인수인계](V90-R1-HANDOFF-2026-09-21.md)를 먼저 읽는다.
V86 원본 없이 V81/V4 기반의 다리별 들림 기능을 이어가는 별도 작업이다.
사용자 확인 후 원점 확인 → Landing 도착 → 토크 OFF → V90-R1 OTA를 완료했다.
설치 후 Landing 오차25틱, 다리별 설정/readback 및 12개 토크 OFF를 확인했다.
보정값은 모두0mm. 실기 보행은 미시험이다. 앞 J2 모드 레지스터 차이와
토크 해제 후 custom 표시에 관한 검증 한계는 위 인수인계 문서를 참고한다.
아래의 설치 중단 및 V82 표기는 그 이전 진행 기록이다.

**2026-09-21 설치 전 실기 조회: V86 / attitudepd_v6 발견, V82 설치 중단.**
사용자 설치 요청 후 읽기 전용 syncstate에서 `rev=attitudepd-v6-v86`,
`profile=attitudepd_v6`, `pose=custom error=141 torque=off safety=ok fault_code=0`
를 확인했다. capability에 V5/V6/originconfirm이 있고 footlift는 없다.
Landing/Relax/OTA 명령은 보내지 않았다. 다시 fetch/원격 브랜치 조회했지만
origin/develop은 여전히 37544b0(V81), main도 기존 커밋이다.
V86 원본 소스를 확보하여 이번 UI/보정 옵션을 통합한 뒤 새 버전으로 설치해야 한다.
아래 V4 앞뒤 높이 분석은 V86/V6 보행을 분석한 결과가 아니다.

**2026-09-21 추가: 다리별 발 들림 UI / V82 준비, 실기 미설치.**
[다리별 추가 들림 문서](FOOT-LIFT-V82-2026-09-21.md)를 먼저 읽는다.
Windows 0.1.5와 iOS 빌드 52 소스에 로봇 위에서 본 FL/FR/RL/RR 설정 UI를 추가했다.
각 0~10mm의 스윙 높이 보정이며 기본값은 0이다. 모든 보행에 공통 적용하고
모델 전환에도 유지한다. 로봇 RAM 설정 + 앱별 로컬 저장이며 자동 재적용은 없다.
V82 펌웨어 준비와 호스트/제어 루프 검증을 완료했으나 실기 설치/보행 검증은
아직 하지 않았다. 아래 V81 설치 완료 기록과 구분한다.

**직전 설치 완료: Windows 0.1.4 + STM32 V81 + ESP32 전용 제어 채널.**
사용자가 로그를 유지하고 제어 통신을 콘솔과 분리하도록 요청했다.
[V81 제어 채널 문서](CONTROL-CHANNEL-V81-2026-09-18.md)를 먼저 읽는다.
기존 0.1.3의 5초 대기 후 재조회는 구형 연결의 호환 경로로만 남긴다.
107개 호스트 검사 및 최종 전송/파서 관련 22개 검사 통과, 세 패키지 빌드 완료.
실기 설치/조회 결과는 `artifacts/control-channel-v81/`에 기록한다.
현재 설치 상태: STM32 V81, ESP32 전용 제어 브리지, Windows 0.1.4 설치 완료.
ESP32 첫 OTA는 60% 이후 ACK 시간 초과였으나, 사용자가 요청한 무선 재시도는
Landing 도착(오차 26틱) → 12개 토크 OFF 재검증 후 100% 전송/무결성 검증에 성공했다.
증거: `artifacts/control-channel-v81/bridge-retry-02/`.
새 제어 채널 실기 조회 검증도 통과했다(`hardware-control-query.json`).
로그 출력이 남은 상태에서 syncstate 응답/DONE이 0.266초에 완료됐고,
이후에도 콘솔 로그가 계속 수신됐다. Landing·토크 OFF·safety OK를 확인했다.
실제 보행 정지 후 재조작 시험은 아직 하지 않았다. 첫 OTA 중단 원인은 미확정이다.

## 다음 컴퓨터에서 이어가기 — 2026-09-18 V81

### 현재 설치와 실행

- 실제 STM32: `attitudepd-v4-v81`, 보행 프로필 `attitudepd_v4`.
- ESP32: 전용 제어 RX UUID 끝자리 `005`, TX indication `006`이 있는 브리지 설치 완료.
- Windows: 0.1.4 설치·실행 완료. 이 PC 설치 경로는
  `%LOCALAPPDATA%/Programs/SpotOMG/V4-0.1.4/SpotOMGController.exe`.
  바탕 화면/시작 메뉴 바로가기도 이 버전을 가리킨다.
- 마지막 실제 조회: Landing, torque OFF, safety OK, fault_code=0.
  다음 작업 때는 현재 상태를 다시 조회한다. 자동 보행 재개는 하지 않는다.
- iPhone: 빌드 51 수정 소스 포함. 이번 전용 제어 채널의 iOS 클라이언트는
  아직 구현하지 않았으며, 실제 iPhone 설치 완료로 해석하면 안 된다.

### 해결 내용과 검증 범위

V4는 전진/후진 모두 같은 반 입력/최대 입력 보행 정책을 사용한다.
V81 통신 변경은 콘솔 로그를 유지하면서 제어 요청/응답을 분리한다.
Windows는 신규 BLE 특성과 MCU `controlv1` 능력을 모두 확인한 뒤 전용 채널을
사용하며, 요청 ID가 일치하는 DONE으로 완료를 판정한다. 콘솔 프롬프트나
오래된 응답은 현재 제어 명령을 완료시키지 않는다.

107개 호스트 검사와 후속 관련 검사, STM32/ESP32/Windows 빌드, 패키지 시작
검사 통과. 후속 검사 개수는 중복되므로 합산하지 않는다.
실기 설치 후 조회에서 help 0.046초, syncstate 0.266초, gaitdiag 0.109초의
제어 완료를 관측했다. syncstate 완료 후에도 콘솔 출력이 계속 도착했다.
이는 조회 1회 검증이며 보행 정지 지연이 해결됐다는 전체 동작 검증은 아니다.
로그 버퍼는 유한하며 초과 시 손실량을 표시한다. 무제한 무손실 보장은 아니다.

### 다음 작업 우선순위

1. 실제 로봇 상태와 바닥 시험 준비를 확인하고 Windows 0.1.4를 연결한다.
   앱에 `전용 제어 채널` 표시가 있는지 확인한다.
2. 중간 전진 → 최대 전진 → 정상 정지 → 다음 조작을 시험하여 정지 후
   컨트롤러가 막히지 않는지, 콘솔 로그가 계속 표시되는지 기록한다.
3. 마우스를 계속 누르는데 `@S`가 발생했던 별도 문제는 실기에서 아직
   원인 확정/재현되지 않았다. 입력 해제 이유·시각 로그와 실제 마우스 상태를
   함께 확인한다. 모의 11초 유지 시험 성공만으로 해결됐다고 판단하지 않는다.
4. iOS에도 같은 분리가 필요하면 제어 특성/요청 ID/DONE 처리와 구형 연결
   호환을 구현하고 Mac에서 빌드·실제 설치/정지 시험을 별도로 수행한다.

읽기 전용 확인 도구는 `scripts/hardware/verify_control_channel.py`이다.
앱 연결을 해제한 상태에서 저장소 루트의 PowerShell에서 실행한다.
출력 경로는 기존 증거를 덮어쓰지 않는 새 이름으로 지정한다.

```powershell
$env:PYTHONPATH='.;tools/servo_tool'
python -u -m scripts.hardware.verify_control_channel --output artifacts/control-channel-v81/readonly-next.json
```

필요 패키지/새 PC 빌드 절차는 `apps/windows/README.md`를 따른다.
하드웨어 업데이트는 AGENTS.md의 Landing 실제 도착 → 토크 OFF 절차를 따른다.
성공한 V81/브리지를 검증 목적으로 다시 설치할 필요는 없다.

### 증거 위치

- `artifacts/control-channel-v81/verification.json`: 전체 검증 수준/이미지 해시.
- `hardware-install/recheck-01/verify.json`: V81 설치 후 Landing/설정 보존.
- `bridge-retry-02/prepare.json`, `install.json`, `ota.txt`: 무선 재시도 성공.
- `hardware-control-query.json`: 실기 제어 응답과 독립 콘솔 수신 시각.
- `bridge-install.json`, `bridge-ota.txt`: 첫 실패 기록. 재시도 성공으로 삭제하지 않는다.

---

아래 내용은 V80/0.1.3 작업 당시의 기록이다.

**추가: Windows 0.1.3 설치 완료, 기존 앱 종료 후 실행 대기.**
0.1.2에서도 정상 Stop 후 ID11 진단 중간에서 출력이 끊겨 프롬프트 시간 초과가
재발했다. 0.1.3은 정상 Stop ACK 이후에 한해 상태 재조회를 한 번 시도하고,
새 상태와 프롬프트가 모두 도착해야 복구한다. 예약 동작/자동 보행 재개는 없다.
Windows 85검사 통과. 실제 BLE 진단 조회는 0.61초에 끝까지 도착했으므로
항상 느린 BLE라는 결론은 철회한다. 계속 누른 중간→최대 입력에서 최초 `@S`가
발생한 원인은 미확정이며, Qt+통신 통합 시험의 11초 유지에서는 재현되지 않았다.
원인별 입력 해제 로그를 추가했다. 실제 0.1.3 재보행 검증은 미실시다.
iOS 빌드 51에는 이번 출력 누락 복구가 아직 없다. 자세한 기록:
[0.1.3 재발 분석](WINDOWS-STOP-DISCONNECT-2026-09-18.md).

[2026-09-18 V4 전후 공통 2단 보행](ATTITUDEPD-V4-COMMON-DIRECTION-GAITS-2026-09-18.md)을 먼저 읽는다.

추가: [Windows 정상 Stop 후 연결 해제 수정](WINDOWS-STOP-DISCONNECT-2026-09-18.md).
V80 정상 정지 후 진단 출력을 받던 중 앱의 고정 5초 제한으로 연결을 닫는 문제를
로그 재생으로 재현했다. Windows 0.1.2를 설치·실행하고 바로가기를 갱신했다.
77개 Windows 검사 + MuJoCo Stop 회귀 1개 통과, 실제 BLE 재보행 검증은 미실시다.
설치 위치는 `%LOCALAPPDATA%/Programs/SpotOMG/V4-0.1.2/`다.

현재 작업 소스 기본은 `attitudepd_v4`, 새 펌웨어 빌드는 `attitudepd-v4-v80`이다.
사용자 요청대로 반 조이스틱에서 V3 전진형, 끝에서 V3 후진형을 전진/후진
공통으로 적용하며 중간은 연속 전환한다. Windows 실행 파일 갱신,
호스트 73개 통과, 4조건 보행과 단계/방향 전환·Stop 시뮬레이션 완료.
기존 4개 모델 10,332조건 목표·주기는 정확히 보존했다.
**실제 로봇에 V80 설치 완료.** 설치 전 Landing 오차 16틱 → 토크 OFF → OTA,
설치 후 V4 선택·Landing 오차 25틱·12개 서보 영구 설정 보존을 확인했다.
마지막은 토크 OFF·safety OK·fault 0. 토크 해제 후 86틱으로 안착하여 자세
표시는 `custom`이며, 도착 실패와 혼동하지 않는다. V4 Stand/보행 실기 시험은
아직 하지 않았다. 증거: `artifacts/attitudepd-v4/hardware-install/attempt-02/`.

**iPhone은 빌드 51 소스 준비, 실제 설치는 Mac 연결 대기다.** 기존 기기는
0.5.0 (49)이며 V4를 모른다. Mac에서 `python3 scripts/hardware/install_ios_v4.py`로
빌드·설치·버전 확인을 진행한다. 스크립트는 Windows 문법/dry-run만 검증했다.
`artifacts/attitudepd-v4/ios-v4-build51-source.zip`은 소스이며 IPA가 아니다.
기존 iPhone 앱의 기본 선택은 연결 후 V3로 바꿀 수 있으므로 구분한다.

[iPhone Stop 연결 해제 수정](IOS-STOP-DISCONNECT-2026-09-18.md)도 빌드 51에 포함했다.
Windows와 같은 고정 5초 종료 로그 제한을 수정하고 실제 로그 fixture를 쓰는
XCTest 6개를 추가했다. Swift/Xcode 부재로 컴파일·XCTest 실행은 아직 못 했으므로
Mac에서 전체 RobotCommandTests를 먼저 실행한 뒤 설치한다.

작업 시작 기준 커밋은 `2450d32`(2026-09-18 03:08 KST), 당시 펌웨어는
`attitudepd-v3-v79`, 모델은 `attitudepd_v3`였다. 이번 V4 변경은 아직 커밋하지 않았다.
[V3 설치 기록](ATTITUDEPD-V3-GAIT-STAND-2026-09-18.md)을 함께 읽는다.
Stand와 보행 대기 자세를 B로
통일했다. 직전 [V2 실기 8걸음](ATTITUDEPD-V2-FRONT-CLEARANCE-2026-09-18.md)은
정상 종료했고 사용자가 양호하다고 평가했다.

9월18일 이 컴퓨터에서 재개하며 사용자도 “보행은 실기에서 잘 되었어”라고
확인했다. 해당 추가 관찰의 정확한 모델/버전은 지정되지 않았으므로 V3의
별도 Stand 유지 시험까지 완료한 것으로 확대하지 않는다. 처음에는 사용자
요청대로 시뮬레이션부터 확인했고, 이후 설치 요청으로 V80을 실기에 반영했다.
후속 V4는 사용자가 명시한 전후 공통 보행 선택 변경이며 V3를 보존했다.

재개 후 [V3 전후진 속도 차이](ATTITUDEPD-V3-DIRECTION-SPEED-2026-09-18.md)를
분석했다. 이전 회전용 설정이 후진에도 적용되어 후진 발 교대가 28.57%
빠르다. 다만 추정 MuJoCo의 몸체 이동은 전진이 더 빨라 사용자 실물 관찰을
재현하지 못했다. 양자를 혼동하지 않는다. 보행 파라미터/펌웨어 변경이나
실기 구동 없이, 네 조건의 속도·접촉 원문과 Windows 호스트 63개 통과를 기록했다.

[V77 인수인계](HANDOFF-2026-09-17-V77-STANDARD-HEIGHT-BALANCE.md)는 과거
`s_native_v6_2_7`의 원인 분석이다. 그 실패 결과를 현재 V2/V3의 실기 결과에
그대로 적용하지 않는다. `artifacts/imu-trace-v78/`의 과거 미설치 진단 후보와
실제로 설치·시험된 `attitudepd-v2-v78`도 다른 바이너리다.
