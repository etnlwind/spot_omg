# 이 저장소에서 작업할 때

서보 위치, 관절 방향/부호, Stow/Landing, 실기·시뮬레이터 좌표 변환을 변경하기 전에
[STS3250 위치 제어 실기 기준](docs/STS3250-POSITION-CONTROL.md)을 읽고 반영한다.
사용자가 같은 문제를 반복하지 않도록 다음 구현에서도 참고하라고 요청한 기록이다.

설정 readback, 호스트 테스트, 실제 위치 유지 시험, 전체 동작 시험의 검증 수준을
구분해 보고한다. 실기에서 관측된 제약을 simulator/mock에서도 재현하고 회귀 검증한다.

실기 펌웨어 업데이트 준비(2026-09-16 사용자 요청): 먼저 Landing으로 전환하고
실제 도착을 확인한 다음 토크 OFF 및 설치를 진행한다. 몸체 지지 확인만으로
Stand에서 바로 Relax/업데이트하지 않는다. Landing 전환 실패 시 자동으로
생략해서 업데이트하지 않는다. 설치 후에도 먼저 Landing 상태를 확인한다.

Landing의 기구적 의미(2026-09-16 사용자 확인): J3가 더 이상 접히지 않는
기구적 접힘 끝에 도달하여 토크를 풀어도 자세가 유지되는 기준이다. 단순한
임의 목표각/모터 위치 유지로 해석하지 않는다. 이 끝점에서 더 접는 명령에
움직임이 없다고 가속도·토크 부족으로 판정하지 않는다. J3 응답 시험 전에
실제 접힘/펴짐 방향과 기구적으로 움직일 여유를 확인한다. 코드의 각도값,
서보 원시 틱, 두 링크 사이의 실제 각도를 같은 값으로 취급하지 않는다.

## 보행 모델 추가 규칙 (2026-09-14 사용자 요청)

새 보행 모델은 기존 이름을 덮어쓰지 않고 버전 이름으로 추가한다. 앱 모델 목록 맨 위에 배치하고, 지원되는 연결에서 새 모델을 기본 선택한다. 시뮬레이터 기본 모델도 같이 갱신한다. 펌웨어 프로필 인덱스 순서는 변경하지 않는다.
S는 쿠션 접지점 X가 각 R2(J2) 축 중심 X에 맞는 정지 기준이다. 새 모델은 S에서 출발한다. 직진에서는 RL·FR이 최대 전진일 때 RR·FL은 최대 후진이며 반 주기 뒤 교대한다. 대각선 두 발의 상대 X/Z 궤적과 타이밍을 공유한다. 사진의 투영각을 실제 관절각으로 취급하지 않는다.

V5 출발/정지 요청(2026-09-15): 첫걸음 FR·RL은 함께 내딛고 동시에 착지한다. 이때 FR만 정상 오므림의 2배로 J1을 안쪽으로 움직이며, RL/FL/RR의 J1은 S를 유지한다. 두 번째 걸음에서 FR의 추가 오므림을 풀고 나머지 다리를 정상 오므림으로 전환한다. 2배는 S에서의 기구 J1 각도 변화량이며 발끝 Y 거리의 2배가 아니다. 보행과 발 교대는 이 전환 중에도 멈추지 않는다. 정지 시에는 오므림을 반대로 풀어 S로 돌아온다. 목표 위상 일치와 실제 접지 동기는 따로 검증하고, 기구 간섭을 숨기려고 충돌을 끄지 않는다.

V6.1 정지 보완(2026-09-15): 정지 신호가 오면 제자리에서 대각선 한 쌍씩 “하나, 둘” 내디뎌 S에 맞춘다. 각 쌍은 한 번의 발 옮김에서 J1을 최종 S 목표로 풀고 착지하며, 정지 후 바닥에서 천천히 벌리는 별도 동작을 넣지 않는다. [구현·접지 검증 한계](docs/S-NATIVE-STOP-PLACEMENT-2026-09-15.md)를 함께 참고한다.

시뮬레이터 창은 사용자가 지정한 LG FULL HD 모니터에 표시한다(2026-09-14 확인).
가상 로봇 시뮬레이터의 기본 카메라는 사용자가 제시한 측면 시점으로 시작한다. 로봇 앞쪽이 화면 오른쪽을 향하도록 azimuth=90°, elevation=0°, distance=1.05m를 사용하고, 몸체와 발끝 쿠션이 함께 보이도록 추적 중심을 무게중심보다 0.1m 낮춘다. 실행 중 마우스로 시점을 바꾸는 기능은 유지한다.

## 폴더 정리 기준 (2026-09-15 사용자 요청)

시뮬레이터 실행 코드는 `simulation/mujoco/runtime/`, 시험은 `tests/`, 실험 도구는 `scripts/`의 목적별 폴더에 둔다. 설정은 `config/`, 결과 JSON·CSV·그림·영상은 `artifacts/`에 저장한다. 루트나 시뮬레이터 소스 폴더에 실험 결과를 다시 쌓지 않는다. 기존 `virtual_robot.py`, `walk.py` 실행 진입점을 유지하고, 파일 이동 시 Python import·리소스 경로·앱 실행·문서 명령을 함께 갱신한다. 자세한 구조는 [폴더 구조](docs/PROJECT-LAYOUT.md)를 따른다.
