name: spot_omg
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytest
  - setuptools>=68
  - pip:
      - mujoco==3.11.0
      - bleak>=0.22,<2
      - "PySide6>=6.8,<7; sys_platform == 'win32'"
      - Pillow>=10,<13
      - -e ../tools/servo_tool
