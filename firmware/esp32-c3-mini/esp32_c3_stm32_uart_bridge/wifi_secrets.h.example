#pragma once

/*
 * Copy this file to wifi_secrets.h and keep the real credentials local.
 * Separate multiple entries with commas; the bridge accepts up to three.
 */
#define SPOT_WIFI_CREDENTIALS \
  {"SSID1", "PASSWORD1"}
